<footer>
  <nav>
    <ul>
      <li>
        {{#if inSwipe}}
        <button type="button">Swipe</button>
        {{else}}
        <a href="/swipe/show/{{user._id}}"> <button type="button">Swipe</button> </a>
        {{/if}}
      </li>
      <li>
        <a href="/profile/{{user._id}} "><button type="button">Profile</button></a>
      </li>
      <li>
        <a href="/matches/{{user._id}}"> <button type="button">Matches</button> </a>
      </li>
    </ul>
  </nav>
</footer>